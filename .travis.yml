sudo: false
language: perl
addons:
  apt:
    packages:
    - libdb-dev
services:
  - elasticsearch
cache:
  directories:
    - extlib/
install:
  - cpanm --notest git://github.com/gmod/jbrowse.git -l extlib/
  - npm install
  - 
script:
  - extlib/bin/flatfile-to-json.pl --gff test/volvox.gff --trackLabel volvox --trackType CanvasFeatures --nameAttributes name,id,description,note

