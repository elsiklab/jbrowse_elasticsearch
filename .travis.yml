sudo: false
cache:
  directories:
    - extlib/
install:
  - cpanm --notest https://github.com/gmod/jbrowse.git -l extlib/
  - npm install
script:
  - flatfile-to-json.pl --gff test/volvox.gff --trackLabel volvox --trackType CanvasFeatures --nameAttributes name,id,description,note

